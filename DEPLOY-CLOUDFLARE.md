# Deploy Cloudflare

O projeto usa Cloudflare Workers Static Assets.

- Root directory: `/`
- Build command: nenhum
- Deploy command: `npx wrangler deploy`
- Diretório publicado: `./public`
- Produção: branch `main`

Após alterações, valide localmente com `npm run dev` antes do deploy.
